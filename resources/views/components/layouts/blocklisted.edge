<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- seo -->
    <meta name="description" content="Blocklisted System is a system that allows you to block players from the guild.">
    <meta name="keywords" content="Blocklisted, System, Guild, Players">
    <meta name="author" content="Kiratrizon">
    @icon('assets/pecu-logo.png')
    <title>Blocklisted | {{ $title }}</title>

    @vite('resources/ts/app.ts')
    @vite('resources/css/dark.css')
    {{{ $slots.styles ? await $slots.styles() : '' }}}
</head>

<body class="bg-gray-950 min-h-screen text-gray-100" style="background:#0a0a0a;">

<!-- Topbar -->
<header class="bg-black/60 backdrop-blur-md border-b border-gray-800 px-6 py-4 flex justify-between items-center">

    <div class="font-semibold text-gray-200 text-lg flex items-center gap-2">
        {{ request().__('blocklisted_panel') }}
    </div>

    <nav class="flex items-center gap-4 text-sm">
        <a href="/admin"
           class="px-3 py-1 rounded hover:bg-gray-800 hover:text-gray-100 transition text-gray-400">
            {{ request().__('dashboard') }}
        </a>

        <a href="/blocklisted/players"
           class="px-3 py-1 rounded hover:bg-gray-800 hover:text-gray-100 transition text-gray-400">
            {{ request().__('players') }}
        </a>
    </nav>

</header>

<!-- Page Content -->
<main class="max-w-6xl mx-auto p-6 flex flex-col gap-6 relative z-10">

    <!-- Floating background accents -->
    <div class="fixed inset-0 pointer-events-none opacity-10">
        <div class="absolute top-20 left-20 w-40 h-40 bg-gray-500 rounded-full blur-3xl"></div>
        <div class="absolute bottom-20 right-20 w-40 h-40 bg-gray-700 rounded-full blur-3xl"></div>
    </div>

    <!-- Page Header -->
    <div class="flex flex-col gap-2">
        <h1 class="text-3xl font-bold text-gray-100">
            {{ $title }}
        </h1>
        <p class="text-sm text-gray-500">
            {{ request().__('manage_review_blocked') }}
        </p>
    </div>

    <!-- Content Card Wrapper -->
    <div class="bg-black/50 backdrop-blur-md border border-gray-800 rounded-xl p-6 shadow-lg flex flex-col gap-6">
        {{{ await $slots.main() }}}
    </div>

</main>

<!-- Footer -->
<footer class="text-center text-xs text-gray-600 py-4 border-t border-gray-800 bg-black/40 backdrop-blur-md">
    © {{ new Date().getFullYear() }} Blocklisted System
</footer>

</body>
</html>
